version: '2'
services:
  dns:
    image: vulhub/bind:latest
    ports:
    - "127.0.0.1::53/tcp"
    - "127.0.0.1::53/udp"
    volumes:
    - ./named.conf.local:/etc/bind/named.conf.local
    - ./vulhub.db:/etc/bind/vulhub.db
