# SecurityRL - LoRA-based Vulnerability Exploitation RL System
# Python >= 3.10

# ═══════════════════════════════════════
# Core Dependencies
# ═══════════════════════════════════════

# LLM API (OpenAI-compatible SDK, used for Tinker / OpenAI / etc.)
openai>=1.0.0

# Docker SDK
docker>=6.0.0

# HTTP requests
requests>=2.28.0

# Data processing
pandas>=2.0.0
pyarrow>=12.0.0

# YAML parsing
pyyaml>=6.0

# Image processing & OCR
Pillow>=10.0.0
pytesseract>=0.3.10

# ═══════════════════════════════════════
# SkyRL (install separately)
# ═══════════════════════════════════════
# git clone https://github.com/NovaSky-AI/SkyRL.git ~/SkyRL
# cd ~/SkyRL && pip install -e .

# ═══════════════════════════════════════
# Vulhub (install separately)
# ═══════════════════════════════════════
# git clone https://github.com/vulhub/vulhub.git ~/vulhub
